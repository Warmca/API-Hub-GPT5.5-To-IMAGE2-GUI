# 必需 —— HTTP 请求；调中转、调润色、SSE 流式解析都靠它
requests>=2.28

# 可选（强烈推荐）—— 参考图本地缩图 + 自动转 JPEG/PNG
# 没装也能跑，但参考图会按原始尺寸 base64 上传，体积大、容易把中转的 SSE 超时撑爆
Pillow>=10.0

# 可选 —— 解析 .env 文件
# 没装时项目内置了一个极简 .env 解析器，功能等价，所以这个其实可不装
python-dotenv>=1.0
